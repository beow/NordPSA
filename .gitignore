# Generated data — never commit
data/raw/
data/processed/
results/
temp/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
*.egg

# Notebooks
.ipynb_checkpoints/
# Arbetsyta-notebook — cellkällorna versioneras i notebooks/cells/
notebooks/explore.ipynb

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
# WSL/Windows-nedladdningsrester
*:Zone.Identifier

# Env
.env
*.log

# Genererade översiktsfigurer (per-run, regenereras ur results/)
docs/nordpsa_overview*.png

# Externa källdokument (stora binärer — hämtas från källan, versioneras ej)
docs/*.pdf

# Kalkylblad — arbetsmaterial och Excels låsfiler (~$…). OBS: gitignore påverkar
# INTE redan spårade filer; docs/batch17_mastersheet.xlsx och
# docs/batch19_mastersheet.xlsx är committade sedan tidigare och fortsätter
# spåras. Kör `git rm --cached <fil>` om även de ska sluta versioneras.
*.xlsx
~$*

# Claude Code, lokala artefakter
.claude/worktrees/
.claude/scheduled_tasks.lock

# Arbetsmaterial som medvetet lämnas utanför versionshanteringen (2026-08-16).
# Filerna ligger kvar på disk — det här döljer dem bara ur `git status`.
# applicerade patchar; koden är committad, filen är historik
patches/
# SDDP-spårets CSV:er (spåret parkerat, se minnet)
sddp/
# kalibreringssvepets mellansteg; den kalibrerade vinnaren är committad
config/archive_terminal_curve_sweeps/
# skärmklipp och valideringsbilder
docs/*.png

